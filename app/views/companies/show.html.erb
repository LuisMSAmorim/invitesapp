<%= render @company %>

<div>
  <%= link_to I18n.t('views.common.edit'), edit_company_path(@company) %> |
  <%= link_to I18n.t('views.common.back'), companies_path %>

  <%= button_to I18n.t('views.common.destroy'), @company, method: :delete %>
</div>
